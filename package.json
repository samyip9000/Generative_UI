{
  "name": "generative-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Weather-aware greeting whose UI is generated by an AI model on each page load.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
